# DESIGN.md

个性化界面定义

```
https://github.com/VoltAgent/awesome-design-md/tree/main
```

# AI_CONTEXT.md

在项目中，`AI_CONTEXT.md` 主要充当“唯一事实来源”（Single Source of Truth），其核心作用是通过复盘经验并详细描述项目需求，确保技术与产品逻辑的高度统一 。其常用作用总结如下：

1.  **统一技术栈与架构规范**：它明确了项目的基础框架（如 Electron、React）、样式方案（Tailwind CSS）及组件库（Shadcn/ui）的使用标准。通过规定禁止直接修改 UI 源码等原则，确保代码的可维护性。
2.  **安全与性能约束**：针对 Electron 等复杂架构，该文件固化了安全沙箱策略（如 `contextIsolation`）和 IPC 通信规范。它还定义了动效时长、位移方式等性能指标，以预防潜在的安全风险和性能掉帧 。
3.  **AI Agent 指令集与上下文注入**：它是高效 AI Coding 的核心工具。通过注入该文件，AI Agent（如 Cursor）可以理解项目的目录结构、不准硬编码等负向约束，从而生成更符合项目预期的代码，减少逻辑偏差。
4.  **数据契约与接口定义**：规范了主进程与渲染进程之间的数据交换格式（如 Word Object Schema）。这种数据契约的固化，确保了多进程协作时字段的一致性。
5.  **产品逻辑与视觉细节固化**：它定义了交互逻辑（如“释义即 Tab”）和视觉联动规范（如关键词颜色跟随背景色色块）。通过总结积累的经验，它能确保产品在多次迭代中保持极简主义和功能一致性。

# PROMPT.md

固化项目中经过验证的高质量提示词（Prompt）。这能确保在进行功能扩展或 Bug 修复时，AI 输出的代码风格和逻辑保持高度一致。

# EXAMPLE.md

提供核心业务逻辑的典型数据示例（如单词 JSON）或代码样板。当 AI 需要生成新功能时，可参照此文件中的模式进行“少样本学习”（Few-shot Learning）。

# API.md

详细记录主进程与渲染进程间的 IPC 接口契约、第三方服务（如 DeepSeek API）的调用参数及返回结构。

# STORAGE.md

专门描述本地数据库（如 SQLite）或加密存储的表结构、键值对定义。

